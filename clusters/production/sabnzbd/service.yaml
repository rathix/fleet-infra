apiVersion: v1
kind: Service
metadata:
  name: servarr-sabnzbd
  namespace: servarr
  labels:
    app: sabnzbd
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sabnzbd
