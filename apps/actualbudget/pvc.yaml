apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actualbudget-actualbudget-data
  namespace: actualbudget
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
  labels:
    app.kubernetes.io/name: actualbudget
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${storage_class}
  resources:
    requests:
      storage: 10Gi
