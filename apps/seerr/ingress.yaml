apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seerr
  namespace: servarr
  labels:
    app.kubernetes.io/name: seerr
spec:
  ingressClassName: traefik-system-traefik
  rules:
    - host: seerr.kennyandries.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: seerr
                port:
                  number: 5055
